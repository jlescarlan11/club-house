<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Join the Club</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="flex flex-col h-screen bg-primary">
    <%- include("partials/header") %>

    <main class="container mx-auto max-w-2xl pt-24 flex-1">
      <div class="bg-secondary rounded-2xl shadow-lg p-8">
        <h1
          class="text-4xl sm:text-5xl font-bold text-text text-center mb-6 flex items-center gap-2"
        >
          Enter Secret Passcode
        </h1>

        <%- include('partials/errors') %>

        <form action="/membership-form" method="POST" class="space-y-6">
          <div class="space-y-4">
            <label for="passcode" class="block text-sm font-medium text-text">
              Passcode
            </label>
            <input
              type="password"
              id="passcode"
              name="passcode"
              class="w-full bg-primary text-text border border-secondary rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-accent"
              placeholder="Enter secret passcode"
              required
            />
          </div>

          <button
            type="submit"
            class="w-full bg-accent text-primary py-3 px-6 rounded-xl font-medium hover:bg-accent/90 transition-colors flex items-center justify-center gap-2"
          >
            <span class="material-symbols-outlined">verified</span>
            Join the Club
          </button>
        </form>

        <div class="mt-6 text-center">
          <a
            href="/dashboard"
            class="text-text/80 hover:text-text transition-colors flex items-center justify-center gap-2"
          >
            <span class="material-symbols-outlined">arrow_back</span>
            Back to Dashboard
          </a>
        </div>
      </div>
    </main>
  </body>
</html>
